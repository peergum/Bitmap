language: cpp

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y nodejs
  - sudo npm install -g particle-cli

install:
    - sudo npm install -g particle-cli

env:
    - PARTICLE_PLATFORM=electron
    - PARTICLE_PLATFORM=photon
#
# install: make
#
# script: bundle exec make all
