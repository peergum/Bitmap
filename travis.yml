before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y nodejs
  - sudo npm install -g particle-cli

env:
    - PARTICLE_PLATFORM=electron
    - PARTICLE_PLATFORM=photon

install: make

build: bundle exec make all
